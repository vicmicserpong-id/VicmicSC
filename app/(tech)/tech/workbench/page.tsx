import { ComingSoon } from "@/components/shared/coming-soon";

export const metadata = { title: "Workbench Teknisi" };

export default function WorkbenchPage() {
  return <ComingSoon title="Workbench Teknisi (FIFO)" phase="FASE 8" />;
}
